// OCR stub. Replace with ML Kit or native Vision OCR.
// Example options:
// - expo-mlkit-ocr (community)
// - react-native-vision-camera + text-recognition plugin

export type OCRResult = { text: string };

export async function extractTextFromImage(uri: string): Promise<OCRResult> {
  // Placeholder: return empty result until wired to real OCR.
  return { text: '' };
}

